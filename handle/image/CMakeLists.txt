if (CMAKE_BUILD_TYPE STREQUAL "Release")
    file(GLOB CURRENT_HEADER LIST_DIRECT false RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.h)
    install(FILES ${CURRENT_HEADER} DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}/handle/image/)
endif ()